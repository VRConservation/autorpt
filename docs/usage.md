# Usage

To run in terminal with local markdown and excel files, go to reports folder and modify the content.md file then upload a budget file called budget.xlsx. Then run

```bash
auto
```

To run from terminal in gui-based app using localhost:8080:

```bash
auto start
```

## Use in a project

```bash
import autorpt
```